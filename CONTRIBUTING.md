# Contributing

Contributions must preserve the truth boundary:

- no `sorry` in accepted theorem files;
- no global complexity claims without formal proof;
- every new executable rule must include a local soundness theorem;
- every decoder must include a decoder soundness theorem;
- every runtime optimization must preserve the trace format semantics.
